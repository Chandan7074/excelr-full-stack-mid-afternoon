class G
{
	public static void main(String[] args)
	{
		String s1 = "hello";
		int i = s1.length();
		System.out.println(i);
		System.out.println(s1.length());
	}
}
/*
* string is also an array of character
* String index start 0, 
* we can able to find out the length of string using lenght() method.
* length method return type is int 
* in case of Arrays length is the property but in case of string it is a method.
* in case of Arrays length is used for finding out the size of the array.
* in case of Stirng length is used for finding out the number of characters available
inside the string object.
* in case of Arrays lenght should not end with parenthesis but in case of strings it should.
*/